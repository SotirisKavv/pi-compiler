/* A complex program containing arrays, modulo and power operators */

func begin() {
  //var numbers int[];
  var i int;
  for(i = 0; i < 5; i=i+1) {
    if (i%2==0)
        continue;
    writeInt(i**3);
    writeString("\n");
  }
}
